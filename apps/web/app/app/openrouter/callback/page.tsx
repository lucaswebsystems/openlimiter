import { OpenRouterCallbackView } from "./openrouter-callback-view";

/**
 * The OpenRouter OAuth callback route. See openrouter-callback-view.tsx's doc
 * comment for why the actual component lives there and this file takes
 * nothing.
 */
export default function OpenRouterCallbackPage() {
  return <OpenRouterCallbackView />;
}
